# Chave da OpenAI para geração de texto por IA.
# Sem essa chave, o app roda em "modo demo" (texto gerado por modelo local de exemplo).
OPENAI_API_KEY=

# Modelo opcional (padrão: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
